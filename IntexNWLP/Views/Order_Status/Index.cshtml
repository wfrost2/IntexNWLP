@model IEnumerable<IntexNWLP.Models.Order_Status>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.statusDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.statusDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.orderStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.orderStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.orderStatusId })
        </td>
    </tr>
}

</table>
