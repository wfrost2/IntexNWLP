@{
    ViewBag.Title = "Index";
}

<h2>Welcome! Choose an option below:</h2>

<div class="row empPortal">
    <div class="col-md-12 portalOption">
        <a>@Html.ActionLink("Reports", "#", null, new { @class = "btn btn-default portalButton" })</a>
    </div>

    <div class="col-md-12 portalOption">
        <a>@Html.ActionLink("Employee", "#", null, new { @class = "btn btn-default portalButton" })</a>
    </div>
</div>

